# Day-30 status report — 2026-05-23

**Project kickoff:** 2026-05-21. **Filed:** 2026-05-23 (day 2 from kickoff).
**Plan reference:** §16 day-30 acceptance criteria.

## Addendum (2026-05-23, after first push) — rows 1+2 flip to ✅

The 8-commit series landed on `origin/main` at
`cf543ee`. The first CI run produced the following result at
https://github.com/hermannsinghh/Mimic/actions:

| Workflow | Trigger on push | Status |
|---|---|---|
| `test` | yes | **✅ #1 success** at `cf543ee` |
| `license` | yes | **✅ #1 success** at `cf543ee` |
| `determinism` | yes | **✅ #1 success** at `cf543ee` |
| `bench` | no (PR + nightly cron) | not run on push — will run on next PR or at 06:00 UTC tomorrow |
| `release` | no (tag push only, `v*`) | not run — first tagged release is the trigger |
| `fibo-bump` | no (quarterly cron + workflow_dispatch) | not run — fires 1st of every 3rd month at 08:00 UTC |
| `sbom` | no (release.published) | not run — fires on GitHub release publish |

**Result.** 3 of 3 push-triggered workflows green; the other 4 are
correctly silent because their triggers (PR / tag / cron / release)
haven't fired yet. **Rows 1 and 2 below flip to ✅ on the strength of
that first run.** The remaining 4 workflows earn their evidence the
first time their natural trigger fires (next PR opens `bench`, first
`v0.2.0` tag opens `release` + `sbom`, quarterly cron opens
`fibo-bump`).

The next status report (day-90, 2026-08-19) should restate the
workflow table — by then `bench` will have run on PRs, and ideally
a `v0.2.x` tag will have fired `release` and `sbom` for the first
time.

Filing 28 days early. Six of the eight day-30 acceptance items are
cleared (1, 2, 3, 4, 6, 8 — rows 1 and 2 flipped to ✅ later this
day after the first CI run, see addendum above); two have real
outstanding work (5, 7).
The pace reflects that the pre-2.0 baseline (282 passing tests) had
already retired most of the spadework on the schema, determinism, and
contagion sides — the 30-day window has been spent restructuring,
landing the F-01..F-12 contract surfaces, and proving the day-30 demo.

---

## Plan §16 day-30 acceptance checklist

| # | Criterion | Status | Evidence |
|---|---|---|---|
| 1 | Monorepo restructured; CI green | ✅ | [`packages/`](../../packages/) layout matches Plan §1.1; 7 required CI workflow files live in [`.github/workflows/`](../../.github/workflows/); `pyproject.toml` declares the uv workspace with 8 members. **First CI run (2026-05-23 after push) green**: `test #1`, `license #1`, `determinism #1` all success at `cf543ee` (see addendum above; the other 4 workflows have non-push triggers). |
| 2 | BSL + change-date in every package; `license.yml` passing | ✅ | Each BSL package carries `LICENSE`, `LICENSE-BSL`, `LICENSE-CHANGE-DATE`, `NOTICE`; the [`license.yml`](../../.github/workflows/license.yml) definition checks presence + no-GPL. **First run on `cf543ee` succeeded** (see addendum above). |
| 3 | `scenario.yaml v1` finalized; svb-replay-2023 parses + validates | ✅ | [`mimic.framework.scenario.spec.load_spec`](../../packages/mimic-framework/mimic/framework/scenario/spec.py) validates `apiVersion: mimic.scenario/v1`; the 7 first-party scenarios scaffolded under [`scenarios/`](../../scenarios/) all parse. |
| 4 | EN clearing vector in mimic-world; golden tests pass | ✅¹ | [`mimic_world.contagion.eisenberg_noe`](../../packages/mimic-world/mimic_world/contagion/eisenberg_noe.py) at 98% coverage; golden vectors at [`tests/determinism/golden/`](../../tests/determinism/golden/). Correction recorded in ADR [`2026-05-21-en-golden-correction.md`](../../decision-record/2026-05-21-en-golden-correction.md). ¹ See footnote 1: adjacent Plan §3.3 W-02 `neva` benchmark for DebtRank is NOT done. |
| 5 | Toto 2.0 adapter in mimic-forecast | ⚠️ | [`mimic_forecast.Toto2Adapter`](../../packages/mimic-forecast/mimic_forecast/) implements `ForecasterAdapter` and raises `ImportError` until upstream weights are available. **Block class: availability unclear, not "waiting on response."** `pip index versions toto-llm` and `toto-forecast` both fail (no PyPI distribution); DataDog's `github.com/DataDog/toto` repo hasn't surfaced a tagged release we can pin. Workaround exists — stub against the published architecture and run on synthetic data, mark the badge "pending real weights" — and needs its own ADR (item 3 below). |
| 6 | Concordia forked & integrated as `mimic.framework.agents.concordia_runtime` | ✅ | F-12 closed today. ConcordiaPersonaBuilder + LM adapter + embedder + 17 tests at [`packages/mimic-framework/mimic/framework/agents/concordia_runtime/`](../../packages/mimic-framework/mimic/framework/agents/concordia_runtime/). Vendoring strategy locked in ADR [`2026-05-22-concordia-vendoring-strategy.md`](../../decision-record/2026-05-22-concordia-vendoring-strategy.md). |
| 7 | Temporal `ScenarioRunWorkflow` runs svb-replay-2023 e2e on laptop | ⚠️ | In-process [`ScenarioRunner`](../../packages/mimic-framework/mimic/framework/scenario/runner.py) executes svb-replay-2023 e2e and proves the contract surface (Decision → propagate_stress → world_state_hash → signed manifest). The Temporal workflow bodies in [`mimic.framework.workflow`](../../packages/mimic-framework/mimic/framework/workflow/) raise `NotImplementedError`. **Block class: engineering, NOT external dependency.** Plan §4.3 sanctions a self-host docker fallback (`temporalio/temporal` image); the work is install the SDK, fill workflow function bodies + activity functions, run a worker process. Tracked as engineering follow-up (item 4 below). |
| 8 | `world_state_hash` reproducible across two consecutive runs (frozen mode) | ✅ | Two paths verified: (a) [`tests/scenario/test_runner_e2e.py::test_two_consecutive_runs_produce_identical_hashes`](../../packages/mimic-framework/tests/scenario/test_runner_e2e.py) — deterministic stub; (b) [`tests/scenario/test_concordia_frozen_run_record_replay.py`](../../packages/mimic-framework/tests/scenario/test_concordia_frozen_run_record_replay.py) — Concordia builder against committed cassettes under `MIMIC_FROZEN_RUN=1`. Bug fix [`2026-05-22-runner-iri-resolution.md`](../../decision-record/2026-05-22-runner-iri-resolution.md) means the hash now actually reflects persona decisions (pre-fix, all actions were silently orphaned). |

**Acceptance score: 6 ✅ / 2 ⚠️** (revised 2026-05-23 after first CI run). Rows 1 and 2 flipped to ✅ on the strength of the first `test`/`license`/`determinism` workflow runs at `cf543ee`. The remaining two ⚠️ rows: one (5) is external availability uncertain (Toto weights); one (7) is unfinished engineering (Temporal worker — self-host fallback exists, not blocked on Temporal Cloud).

¹ **Footnote — `neva` benchmark for DebtRank.** Plan §3.3 W-02 ("DebtRank with provable convergence") and `packages/mimic-world/CLAUDE.md` both call for benchmarking DebtRank against the published `neva` reference implementation for behavioural correctness. No test in the repo references `neva`. The existing golden vectors prove the implementation matches its own goldens — not that the behaviour matches a published reference. This is a real gap that day-30 row 4 quietly elides. Tracked as engineering follow-up item 9 below.

---

## F-12 highlights

F-12 (Concordia fork integration) landed across 2 days in six sequenced
steps. Status doc with the equivalence numbers is at
[`2026-05-22-f12-step5-equivalence.md`](2026-05-22-f12-step5-equivalence.md).
Fallback-to-DeepSeek story is at
[`2026-05-22-f12-deepseek-fallback.md`](2026-05-22-f12-deepseek-fallback.md).

| Step | Deliverable | Status |
|---|---|---|
| 1 | Vendor Concordia v2.0.1 at `packages/mimic-concordia/` | ✅ |
| 2 | `ConcordiaPersonaBuilder(prefab, llm_provider)` with `is_deterministic=False` | ✅ |
| 3 | svb-replay + taiwan-strait cassettes recorded against DeepSeek; Anthropic re-record pending API key | ✅ (DeepSeek) ⚠️ (Anthropic) |
| 4 | Noise floor measured (W1=0, TV=0 on DeepSeek at temp=0) | ✅ — also surfaced harness gap: spec-seed doesn't propagate to LLM `seed` param; recorded for follow-up |
| 5 | Equivalence test green against heuristic baseline | ✅ — W1 $8.24M vs $50M (83.5% margin); TV 0.167 vs 0.20 (16.7% margin) |
| 6 | Land — node_name extraction bug fixed (was silently orphaning every action) | ✅ (code) ⚠️ (Anthropic canonical recording pending) |

**Provider table (Plan §4.2) progress**: 2 of 6 concrete adapters shipped:

| Provider | Model | Tier | Status |
|---|---|---|---|
| Anthropic | `claude-opus-4-7` | T1 | ✅ adapter + 16 tests; default model change recorded in ADR [`2026-05-22-anthropic-model-choice.md`](../../decision-record/2026-05-22-anthropic-model-choice.md) |
| Anthropic | Sonnet 4.6 | T2 | not yet — uses same adapter, needs model id |
| Google | Gemini 3 Pro | T2 | not started |
| xAI | Grok 4 | T1 adj | not started |
| OpenAI | GPT-5.1 | T2 | not started |
| DeepSeek | V3.2 | T3 | ✅ adapter + 16 tests via OpenAI-compatible API |

---

## Test / coverage snapshot

**Total: 579 passing** across 10 packages and harnesses.

| Package / dir | Tests | Δ vs pre-2.0 |
|---|---|---|
| mimic-framework | 230 | +193 (was 37) |
| mimic-world | 101 | +47 (was 54) |
| mimic-forecast | 49 / 1 fail | +2 (was 47/3; `fredapi` optional dep still missing) |
| mimic-sim | 67 | unchanged |
| mimic-signal | 88 | unchanged |
| mimic-bench | 5 | unchanged |
| mimic-concordia | 7 | NEW |
| hub-client | 11 | NEW |
| eval/harness | 13 | NEW |
| tests/equivalence | 8 | NEW (4 noise-floor + 4 equivalence) |
| **total** | **579** | **+297 over the pre-2.0 baseline of 282** |

The 1 failing test (`mimic-forecast::test_pull_series_fred_success`)
is a pre-2.0 known issue — missing optional `fredapi` dep. Not gating.

**Coverage** (from [`2026-05-21-coverage.md`](2026-05-21-coverage.md);
audit-grade modules verified ≥90%):

| Package | Coverage | vs §13.1 threshold (80%) |
|---|---|---|
| mimic-framework | 93% | ✅ |
| mimic-world | 71% | drag from legacy `visualization.py` / `twin.py` — audit-grade modules at 98-100% |
| mimic-forecast | 42% | drag from model-dep adapters; testable surface at >85% |

---

## Sealed contract surfaces

Nine surfaces have been frozen across §0/§5/§7/§12 and the four
F-12-blocking ADRs. F-12 was authored against these; nothing in F-12
required relitigating any of them.

| # | Surface | Source |
|---|---|---|
| 1 | `Decision` / `Outcome` schema with golden vectors | [`mimic.framework.schema.decision`](../../packages/mimic-framework/mimic/framework/schema/decision.py) |
| 2 | `world_state_hash` Merkle-DAG with golden vectors | [`mimic.framework.determinism.hashing`](../../packages/mimic-framework/mimic/framework/determinism/hashing.py), [`tests/determinism/golden/`](../../tests/determinism/golden/) |
| 3 | `policy_version` PDP-enforced on every Decision | [`mimic.framework.policy.pdp`](../../packages/mimic-framework/mimic/framework/policy/) |
| 4 | `mimic.route` + `mimic.decision` OTEL spans with frozen attrs | [`mimic.framework.routing.telemetry`](../../packages/mimic-framework/mimic/framework/routing/telemetry.py), [`mimic.framework.agents.telemetry`](../../packages/mimic-framework/mimic/framework/agents/telemetry.py) |
| 5 | Audit-grade refusal contract with `is_deterministic` flag | ADR [`2026-05-21-audit-grade-refusal.md`](../../decision-record/2026-05-21-audit-grade-refusal.md) |
| 6 | Equivalence criterion with provenance rule | ADR [`2026-05-21-runner-equivalence-criterion.md`](../../decision-record/2026-05-21-runner-equivalence-criterion.md) |
| 7 | Frozen-run cache: LocalFS + S3 SSE-AES256/KMS | [`mimic.framework.determinism.frozen`](../../packages/mimic-framework/mimic/framework/determinism/frozen.py) |
| 8 | Cache-key formula pinned by regression test | [`tests/determinism/test_cache_key_composition.py`](../../packages/mimic-framework/tests/determinism/test_cache_key_composition.py) |
| 9 | `RecordingProvider` + cassette convention + sidecar metadata | [`tests/fixtures/frozen-run/README.md`](../../tests/fixtures/frozen-run/README.md) |

---

## ADRs

Seven decision records on file:

| Date | ADR | Subject |
|---|---|---|
| 2026-05-21 | [audit-grade-refusal](../../decision-record/2026-05-21-audit-grade-refusal.md) | ScenarioRunner refuses non-deterministic builder outside frozen-run mode |
| 2026-05-21 | [connector-queue-deviation](../../decision-record/2026-05-21-connector-queue-deviation.md) | NOAA + GDELT before AM Best + LSEG (procurement-gated reordering) |
| 2026-05-21 | [en-golden-correction](../../decision-record/2026-05-21-en-golden-correction.md) | EN clearing vector golden values |
| 2026-05-21 | [runner-equivalence-criterion](../../decision-record/2026-05-21-runner-equivalence-criterion.md) | W1 + TV thresholds with provenance gate |
| 2026-05-22 | [concordia-vendoring-strategy](../../decision-record/2026-05-22-concordia-vendoring-strategy.md) | Pinned-dep wrapper vs full source vendor for Concordia |
| 2026-05-22 | [anthropic-model-choice](../../decision-record/2026-05-22-anthropic-model-choice.md) | `claude-opus-4-7` as default (plan named the now-deprecated 4-5 alias) |
| 2026-05-22 | [runner-iri-resolution](../../decision-record/2026-05-22-runner-iri-resolution.md) | Walk-up resolution for `instrument_iri → network node`; avoided a schema major bump |

---

## Open items entering day-31

Owners are listed as `solo` since this is a one-person operation today.
Target dates are best-faith — items dependent on outside parties carry a
**condition** instead of a date.

### Procurement / external-key-blocked

| # | Item | Owner | Target / Condition |
|---|---|---|---|
| 1 | **Anthropic canonical cassette recording** — `scripts/record_svb_cassettes.py` with default `MIMIC_RECORD_PROVIDER=anthropic`, against both svb-replay-2023 and taiwan-strait-30d-closure. Single command per scenario. Estimated spend: under $5 total. | solo | **condition:** `ANTHROPIC_API_KEY` available. Then same-day. |
| 2 | **Anthropic canonical noise-floor measurement** — `scripts/measure_concordia_noise_floor.py` with default `MIMIC_NOISE_PROVIDER=anthropic`. | solo | **condition:** same as item 1. Then same-day. |

### Investigation / ADR-pending

| # | Item | Owner | Target / Condition |
|---|---|---|---|
| 3 | **Toto 2.0 availability triage + ADR** — verify whether DataDog has released Toto 2.0 weights publicly; if yes, integrate; if no, write an ADR for the "stub against published architecture, badge pending real weights" workaround. Either path lands an ADR. | solo | 2026-06-06 (≈ 2 weeks; investigation + ADR draft) |

### Engineering follow-ups

| # | Item | Owner | Target / Condition |
|---|---|---|---|
| 4 | **Temporal worker self-host** (Plan §4.3 fallback) — install `temporalio` SDK, fill workflow bodies + activity functions, run `temporalio/temporal` docker, prove equivalence with in-process runner on svb-replay. F-05 acceptance criterion. | solo | 2026-06-20 (day-30 milestone close) |
| 5 | **Threshold provenance for the other 5 prefabs** (BankTreasuryALM, HedgeFundRiskOfficer, CentralBankLiquidityProvider, RatingAgencyAnalyst, BrokerCedentAdvisor) — each needs a domain-matched scenario, a heuristic baseline, recorded cassettes, and a `(theory)`/`(prior-art)`/`(domain)` citation. ReinsurerTreatyPricer is the only one done. | solo | 2026-07-31 (5 prefabs × ~1 week each; partial parallelism) |
| 6 | **Seed propagation through `Prefab.run` and `ConcordiaPersonaBuilder`** — currently the LLM provider's `seed` param always gets `None`, so the noise-floor harness measures spec-seed sensitivity, not LLM stochasticity. ADR-worthy: touches the per-call contract. Documented in [`2026-05-22-f12-deepseek-fallback.md`](2026-05-22-f12-deepseek-fallback.md). | solo | 2026-06-13 (ADR + 1-line plumbing change) |
| 7 | **`eval` namespace `__init__.py`** — local pytest runs need `PYTHONPATH=.` because `eval/` is a namespace package without an `__init__.py`. Add the file or pin via `[tool.pytest.ini_options] pythonpath = ["."]` in root `pyproject.toml`. | solo | 2026-05-30 (cheap; bundle with the git-init commit) |
| 8 | **F-08 S3 backend production wiring** — `mimic.framework.determinism.frozen.S3Backend` lands, stub-tested. Production wiring (real bucket, IAM, SSE-KMS key per environment) pending deploy target decision. | solo | **condition:** lighthouse-deploy target chosen. Then 1 day. |
| 9 | **DebtRank behavioural benchmark against `neva`** (Plan §3.3 W-02, mimic-world CLAUDE.md) — current golden tests only prove DebtRank matches its own goldens; the spec calls for parity with the published `neva` reference implementation. Tracks the day-30 row 4 footnote. | solo | 2026-06-20 (≈ 4 weeks; install `neva`, port a test scenario, compare R values within tolerance) |

### Coverage debt

| # | Item | Owner | Target / Condition |
|---|---|---|---|
| 10 | **mimic-world legacy modules** (`twin.py`, `visualization.py`, `result.py`) drag the package to 71%. Decision pending: backfill tests, or move to `legacy/` subpackage excluded from the §13 threshold. | solo | 2026-07-15 (after F-11 decisions clarify whether legacy still pays rent) |
| 11 | **mimic-forecast adapters at 42%** — model-dep adapters aren't testable without weights. Same decision as item 10. Couples to item 3 (Toto outcome). | solo | 2026-07-15 |

---

## Looking to day-90 (2026-08-19)

Plan §16 day-90 criteria, with current readiness:

| Criterion | Today's state |
|---|---|
| All P0 tasks complete (F-01..F-07, F-12, FC-01..FC-06, W-01..W-04, H-01..H-03) | F-12 done; F-01..F-09 done; F-11 partial; FC-01/02 need weights; H-02/H-03 need infra |
| Routing T1/T2/T3 cascade live; cost guardrail tested | Cascade live; `BudgetExceeded` tested; full T1/T2/T3 provider matrix only 2/3 tiers represented (T1 Anthropic, T3 DeepSeek; T2 still needs an adapter — Sonnet/Gemini/GPT) |
| 5 prefabs shipped with calibration badge ≥0.7 directional accuracy | 6 prefabs scaffolded; ReinsurerTreatyPricer has F-12 equivalence (not yet a calibration badge); 5 others need badges |
| Hub private alpha live; 1 reinsurer pulling signed scenarios | hub FastAPI skeleton lands `/healthz`; needs H-02 OCI backend + H-03 Sigstore for end-to-end signed pull |
| docs.mimic.ai with quickstart | not started |
| SOC 2 Type I evidence ≥80% complete | not started |

The day-90 push therefore has three concentrated lanes:

* **F-11** calibration harness completion (GIFT-Eval, Chronos-ZS, AgentDojo wrappers).
* **H-02 / H-03** hub backend wiring (Harbor or GHCR + Sigstore Fulcio/Rekor).
* **At least one T2 provider** to make the cascade non-degenerate.

Plus all the open items above. Realistic plan is to revisit at day-60
to triage whether SOC 2 evidence collection starts in-window or slips.

---

## How to read this report

This is a checklist-driven document. Every cell either links to a file
or names a blocker. If a row is ⚠️ or ❌, it should be clear from the
linked text what would be needed to flip it to ✅ — that's the contract
for the next status report at day-90 (2026-08-19).
