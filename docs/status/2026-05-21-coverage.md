# Coverage report — 2026-05-21

Per Plan §13.1, coverage threshold is ≥80% on `mimic-framework`, `mimic-forecast`,
`mimic-world`. Snapshot today:

| Package | Coverage | Status |
|---|---|---|
| mimic-framework | **93%** | ✅ above threshold |
| mimic-world | 71% | ❌ 9 pts below |
| mimic-forecast | 42% | ❌ 38 pts below |

## What's actually covered

The audit-grade critical paths are well above 80%:

| Module | Coverage |
|---|---|
| mimic_world/contagion/eisenberg_noe.py | 98% |
| mimic_world/contagion/debt_rank.py | 98% |
| mimic_world/contagion/stress.py | 100% |
| mimic_world/contagion/fibo_builder.py | 89% |
| mimic_framework/schema/* | 100% |
| mimic_framework/determinism/hashing.py | 85% |
| mimic_framework/determinism/seed.py | 100% |
| mimic_framework/determinism/frozen.py | 96% |
| mimic_framework/routing/cascade.py | 94% |
| mimic_framework/policy/pdp.py | 94% |
| mimic_framework/scenario/artifact.py | 92% |
| mimic_framework/scenario/signing.py | 84% |
| mimic_framework/agents/prefabs/* | 94% |
| mimic_forecast/base.py | 100% |
| mimic_forecast/node_api.py | 93% |
| mimic_forecast/ensemble.py | 97% |

## What drags the headline down

**mimic-world (71%):**
- `visualization.py` — 0% (legacy plot helpers, no tests)
- `twin.py` — 26% (legacy Twin entry point, partially exercised)
- `result.py` — 50% (legacy result dataclasses)

These are pre-2.0 narrative/visualization code. The contagion math is at 97–100%.

**mimic-forecast (42%):**
- All legacy adapters (chronos, finbert, kronos, moirai, timesfm, bistro) sit at
  0% because tests require model deps that aren't installed. Same story for the
  new frontier scaffolds (toto, timer_s1, tirex) which are at 92% — covered only
  for the install-message path, not inference.
- `data/series.py` and `data/covariates.py` at 34-38% — legacy yfinance/fred
  helpers (the missing-deps tests).

## What this means for the day-30 gate

The audit-grade contract surface is well-tested. The headline coverage gaps are on
either (a) legacy code that's been demoted/will be pruned (`twin.py`, `visualization.py`,
`result.py`), or (b) adapters that legitimately can't be tested without the upstream
model packages installed.

## Action items

1. Decide whether to prune or backfill tests for `mimic_world/{twin,result,visualization}.py`.
2. Add `pip install 'mimic-forecast[chronos,finbert,timesfm]'` to `bench.yml` so at
   least the existing adapters get exercised in CI; install paths for new frontier
   adapters land with FC-01/02/03.
3. Once both done, headline coverage should clear 80% on all three packages.

CI workflow `test.yml` does not currently enforce the ≥80% threshold — adding
`--cov-fail-under=80` is gated on the above two fixes.
